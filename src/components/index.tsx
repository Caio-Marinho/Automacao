import AuthContainer from "./AuthContainer"; // Importe o componente AuthContainer

const Login = () => {
    return (
        <div>
            <AuthContainer />
        </div>
    );
}

export default Login;
